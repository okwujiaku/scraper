# Universal Option C worker — copy to .env for local runs (never commit .env)

DISCORD_TOKEN=your_discord_user_token
CHAT_ID_CLIENT_1=your_group_chat_id

# Optional
HISTORY_POLL_SECONDS=45
POLL_HISTORY_LIMIT=3
SEND_STARTUP_PING=true
